[package]
name = "nova-exo"
version = "0.1.0"
edition = "2021"

[dependencies]
uart_16550 = "0.2"
libm = "0.2"

[profile.release]
opt-level = 2
debug = true
lto = true
panic = "abort"

[profile.dev]
opt-level = 0
debug = true
panic = "abort"
